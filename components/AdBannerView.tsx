// Stub para web: nunca hay SDK ni banner. Mantiene la firma de la versión
// nativa para que las pantallas no tengan que ramificar por plataforma.
export function AdBannerView(_props: { focused: boolean; placement?: string }) {
  return null;
}
