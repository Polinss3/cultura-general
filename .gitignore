node_modules/
.expo/
dist/
web-build/
.env
.env.local
*.log
.DS_Store
ios/
android/
google-play-key.json

# Claude Code worktrees (each is a nested git checkout, not source code)
.claude/worktrees/

# Build artefacts — never commit these
*.ipa
*.apk
*.aab
build-*.ipa
build-*.apk

# Material de la ficha de App Store: capturas fuente y montajes generados.
# Son cientos de MB de PNG. El script que los reconstruye NO vive aquí dentro,
# porque quedaría ignorado con ellos: está versionado en
# scripts/generar-capturas-store.py.
Capturas App Store/
app-store-assets/

# Volcado intermedio de scripts/mine-year-questions.mjs. Se regenera pasando el
# minador; lo que se versiona es la tabla ya curada de scripts/build-years.mjs.
scripts/mined.json

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli